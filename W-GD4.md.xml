<?xml version="1.0" encoding="utf-8"?>
<document><!-- GD4 -->

<pre><code>section = content.newSection()
box = section.newMain()
</code></pre>

<p><a name="GD4"/></p>
<h2>Coding type specimens <span class="wcode">(GD4)</span></h2>
<h3>Write code to generate type specimens for web and print, using a selection of typefaces.</h3>
<h4>Start June 7<span class="sup">th</span> 2021 • 2 weeks • €290</h4>
<pre><code>box = box.newInfo()
</code></pre>

<p>Design type specimen pages to show the characteristics of a given typeface. What is the glyph set? Which OpenType features are included? What is the quality of spacing and kerning? How does the typeface behave is various sizes and with changing color background? </p>
<ul>
<li>The workshop addresses the sketching of specimen layouts.</li>
<li>Converting those sketches into parameters and algorithms.</li>
<li>Writing code that generates type specimen documents for web (HTML/CSS) and print (PDF). </li>
</ul>
<p>The use of <a href="http://drawbot.com" target="external">DrawBot</a> in this workshop requires a MacOS computer.</p>
<p>Completion of workshop <a href="#TY1">Basic coding in Python #1</a> is recommended but not required.</p>
<pre><code>box = section.newCropped()
</code></pre>

<p><img alt="cover y=center" src="images/LetterproefVanDeGarde_7.png" /></p></document>